# Solo para PRODUCCIÓN (deploy). En local no hace falta: Vite usa su proxy.
# Pon aquí la URL del backend desplegado (sin barra final).
# Ej.: VITE_API_URL=https://xcup-backend.onrender.com
VITE_API_URL=
